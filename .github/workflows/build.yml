name: Build WPF EXE

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore BLMRotationSim.csproj

      - name: Build EXE
        run: dotnet publish BLMRotationSim.csproj -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true

      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: BLMRotationSim-EXE
          path: BLMRotationSim\bin\Release\net8.0-windows\win-x64\publish\*.exe
